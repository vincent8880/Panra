[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python manage.py migrate && python manage.py setup_google_oauth && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

















